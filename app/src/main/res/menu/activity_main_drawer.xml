<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nuevo_item" android:icon="@android:drawable/ic_menu_add"
            android:title="Nuevo Item" />
        <item android:id="@+id/localizaciones" android:icon="@android:drawable/ic_menu_mylocation"
            android:title="Localizaciones" />
        <item android:id="@+id/inventario" android:icon="@android:drawable/ic_menu_agenda"
            android:title="Inventario" />
        <item android:id="@+id/scanAdd" android:icon="@android:drawable/ic_menu_zoom"
            android:title="ScanAdd" />
    </group>

    <item android:title="Informacion Adicional">
        <menu>
            <item android:id="@+id/buscar" android:icon="@android:drawable/ic_menu_search"
                android:title="Buscar" />
            <item android:id="@+id/salir" android:icon="@android:drawable/ic_delete"
                android:title="Salir" />
        </menu>
    </item>

</menu>